import 'package:schedule_builder/schedule_builder.dart';

void main(List<String> arguments) {
  ScheduleBuilder.generateSchedule();
  // ScheduleBuilder.staticName;
  // final ScheduleBuilder instance = ScheduleBuilder(name: 'PABLITO');
  // final ScheduleBuilder instance2 = ScheduleBuilder(name: 'jAUN');
  // instance.name;
  // instance2.name;
  // ScheduleBuilder.staticName = 'Pedro';
  // ScheduleBuilder.staticName = 'Javier';
}
